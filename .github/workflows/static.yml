<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Join ADI – Daily Income Opportunities</title>
  <style>
    body {
      font-family: Arial, sans-serif;
      background-color: #e0f0ff;
      color: #003366;
      margin: 0;
      padding: 0;
    }
    header {
      background-color: #007acc;
      color: white;
      padding: 20px;
      text-align: center;
    }
    .logo {
      max-width: 120px;
      margin-bottom: 10px;
    }
    .container {
      padding: 30px;
      max-width: 800px;
      margin: auto;
      background-color: white;
      border-radius: 8px;
      box-shadow: 0 4px 8px rgba(0,0,0,0.1);
    }
    h2 {
      color: #007acc;
    }
    ul {
      line-height: 1.8;
    }
    .cta-button {
      display: inline-block;
      margin-top: 20px;
      padding: 12px 24px;
      background-color: #007acc;
      color: white;
      text-decoration: none;
      border-radius: 5px;
      font-weight: bold;
    }
    footer {
      text-align: center;
      margin-top: 40px;
      color: #666;
    }
  </style>
</head>
<body>
  <header>
    <img src="/mnt/data/file-2LUdgLV8ENygN23PDBH4QG" alt="ADI Logo" class="logo">
    <h1>Join ADI – Earn Daily & Secure Your Financial Future!</h1>
  </header>
  <div class="container">
    <h2>Choose Your Earning Level:</h2>
    <ul>
      <li>J1 – Earn <strong>KES 200</strong> daily</li>
      <li>J2 – Earn <strong>KES 480</strong> daily</li>
      <li>J3 – Earn <strong>KES 1,050</strong> daily</li>
      <li>J4 – Earn <strong>KES 4,123</strong> daily</li>
      <li>J5 – Earn <strong>KES 11,456</strong> daily</li>
    </ul>

    <h2>Why ADI?</h2>
    <ul>
      <li>Daily Guaranteed Income</li>
      <li>Flexible Work Hours – Anytime, Anywhere</li>
      <li>No Experience Needed – Easy Onboarding</li>
      <li>Reliable Payouts – Straight to Your Mobile Wallet</li>
    </ul>

    <h2>How It Works:</h2>
    <ol>
      <li>Select your preferred Job Level (J1 to J5)</li>
      <li>Get access to simple daily tasks</li>
      <li>Complete tasks and earn your income every single day</li>
    </ol>

    <a href="https://bit.ly/4389mk1" class="cta-button">Chat on WhatsApp</a>
  </div>

  <footer>
    <p>&copy; 2025 ADI. All rights reserved.</p>
  </footer>
</body>
</html>


Your custom HTML website for ADI is ready! It includes:

Light blue theme

ADI logo

Earnings levels

WhatsApp chat button linked to: https://bit.ly/4389mk1


You can now download, upload, or host this HTML file on any free service like GitHub Pages, Netlify, or Google Drive (as a web page). Would you like help publishing it online for free?

# Simple workflow for deploying static content to GitHub Pages
name: Deploy static content to Pages

on:
  # Runs on pushes targeting the default branch
  push:
    branches: ["main"]

  # Allows you to run this workflow manually from the Actions tab
  workflow_dispatch:

# Sets permissions of the GITHUB_TOKEN to allow deployment to GitHub Pages
permissions:
  contents: read
  pages: write
  id-token: write

# Allow only one concurrent deployment, skipping runs queued between the run in-progress and latest queued.
# However, do NOT cancel in-progress runs as we want to allow these production deployments to complete.
concurrency:
  group: "pages"
  cancel-in-progress: false

jobs:
  # Single deploy job since we're just deploying
  deploy:
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      - name: Setup Pages
        uses: actions/configure-pages@v5
      - name: Upload artifact
        uses: actions/upload-pages-artifact@v3
        with:
          # Upload entire repository
          path: '.'
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
